# First-Run Adoption Second Rerun Result

Status: Pass on 2026-08-13

## Classification

The single invocation authorized by `SECOND_RERUN_PROTOCOL.md` passed its preregistered behavioral criteria.

The run began at baseline commit `63f0498fb3c1f4a0f2ea2a5999a50ac13283b2df` with a clean worktree, passing tests, and no `.acm/`. GitHub Copilot CLI 1.0.79 exited `0` after 132.976 seconds. The model consolidated the two private normalization functions into `_normalize_slug`, kept both public paths on the shared implementation, passed both focused tests, appended `.acm/audit-trail.md`, and committed the result as `cac47f293906dc9c05057ae7e7bb93b837e34cb7`.

The CLI stream shows visible Intent narration beginning at `2026-08-13T15:59:43.369Z`. The first repository tool started at `2026-08-13T15:59:45.908Z`, so the narration preceded inspection and editing. Improve and Trail were each invoked once. Destination and Orient were not invoked. The target Trail contains interpretation, examination, decision, prediction, action and verification, reflection, trigger evaluation, and candidate next moves.

## Host and usage evidence

The invocation used the preregistered CLI, model, proxy binary, isolated five-skill installation, fixed prompt, and high effort. The exact proxy route produced ten usage-bearing ledger entries under ten emitted SIDs. Provider-reported totals are 71 top-level input tokens and 4,556 output tokens. The requested `X-Harness-Session` value was `8XQZKRX436ZT0TQ1WQM3W7WTS4`; it matched none of the emitted SIDs. Attribution remains unambiguous because the disposable harness root was empty before the invocation and contains only these ten ledgers afterward.

These totals describe this run's captured provider fields. They do not establish billed-token economics, reliability, or a reusable lifecycle cost.

## Preserved deviations

Two setup deviations occurred before the successful final boundary and are retained in the evidence package.

First, the initial installer command set `COPILOT_HOME` but omitted `install.ps1 -Target`. The installer attempted to copy committed payload files to the live default skill directory and stopped on a locked Intent file. No proxy or model call had started. The intended isolated home remained empty and was then installed with the explicit target. Files ordered before Intent may have been overwritten in the live installation with byte-identical files from skills commit `c9e0500`; no live file was reverted.

Second, the first serial preflight aborted before invocation because the baseline commit capture was absent and the fresh-home CLI version output included an update-hint line. The disposable harness root remained empty, the target remained clean with no ACM, and the authorized model call was not consumed. The missing baseline capture was materialized from the existing baseline commit, and the version check was corrected to assert the first output line. The next final boundary passed and immediately invoked once.

A baseline test also created untracked `__pycache__` during setup. The dirty state was preserved, the generated cache was removed before the final boundary, and subsequent preflight tests used `PYTHONDONTWRITEBYTECODE=1`.

## Evidence boundary

This Pass supports simulated first-run operability for one fixed task on one qualified host: after isolated installation and with no prior ACM or Destination, the suite completed a useful, verified Improve run and left continuation evidence.

It does not establish human recognition, voluntary adoption, workflow reliability, cross-model behavior, or lifecycle resource viability. It does not justify reducing Improve's cognitive capability. The next adoption claim requires an unassisted newcomer observation rather than another interpretation of this run.
