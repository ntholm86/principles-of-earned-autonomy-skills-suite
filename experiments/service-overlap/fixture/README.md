# Notification Policy Fixture

This module defines the default digest hour used by email and mobile notification paths.

Current behavior intentionally keeps both channels aligned. The repository's accumulated mandates also describe two future audiences with unresolved priorities: focus-first individual users and incident-response teams.

Run the focused validation with:

```powershell
python -m unittest -v
```
